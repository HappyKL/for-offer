一面：

k8s调度

docker使用的aufs

docker commit的实现

docker为什么会引起云原生潮

docker的劣势有啥



C++：

为啥C++只能返回一个返回值，而python可以返回多个返回值

内存回收

智能指针的实现



操作系统：

僵尸进程和孤儿进程，孤儿进程有哪些缺点

堆栈是啥



算法：

快速排序如何选择pivot，必须避免最差



网络

七层模型

http2

ping是哪一层 tcp层！！



二面



docker的网络模型：宿主机内两个容器（不在一个网段）如何通信



宿主机1ip为10.21.5.100/24  宿主机2:ip为10.21.5.101/27（掩码本应该是写24，但写错了），这样的情况下，101第一次ping100时会怎么走（经过路由器），第二次ping100时会怎么走（通过网桥二层协议）。为啥？当第一次ping后，返回包时进行了ARP广播，两者都到了自己的ARP里面



docker exec 和 docker 的1号进程关系是什么，如何停止容器（兄弟进程，停止时需要给cgroup中proc文件中所有进程号都发送停止信号，tasks里存放着进程号和线程号，proc文件里存放着进程号）

docker中使用free这种命令时看到的仍旧是宿主机的信息（挂载proc不彻底），如何解决这个问题，（通过cgroup中的字段进行查询资源）资源超过了怎么办？（暂停？）

docker中很多僵尸进程怎么解决，会占用大量的pid资源（可以像linux中一样，使用init进程来定时回收，不能使用docker的一号进程，他很忙）



docker cgroup中有哪些字段？如何进行控制？memory和cpu分别有哪些



三面



一天的安排



时延怎么造成的？（通信时延+调度时延），通信时延不仅包括传输还有协议栈这些时延，两端的协议栈时延可以进行优化



调试工具

查看内存泄露（如何检测野指针，内存泄露？）

查看函数执行时间



c++遇到的错误：段错误怎么解决的？



智能指针

堆排序，怎么排

写一个链表的插入删除



















